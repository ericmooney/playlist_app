<h1>Playlistr</h1>

<% @users.each do |user| %>
<table>
  <thead>
    <tr>
      <th> Playlists for <%= user.name %></th>
    </tr>
  </thead>
  <tbody>
    <%= user.lists.each do |list| %>
      <tr>
        <td><%= list.name %>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>